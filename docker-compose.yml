services:
  prod:
    build:
      context: .
      dockerfile: Dockerfile
    image: codegeekery/codegeekerylink:latest
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  node_modules: